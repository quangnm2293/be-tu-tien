PORT=4000
FRONTEND_URL=http://localhost:3000
# Local: Postgres trên máy. Production (Railway): dùng URL từ plugin PostgreSQL — KHÔNG dùng localhost trong container.
DATABASE_URL="postgresql://user:password@localhost:5432/blog_tu_tien?schema=public"
